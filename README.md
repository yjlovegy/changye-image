# 长夜的绘图器

SillyTavern 剧情配图扩展，基于 **柏柏** 的 [柏宝绘 / ST-BaiBai-Image](https://github.com/baibai-git/ST-BaiBai-Image) 修改，当前版本 **V1.2**（安装版本 `1.2.0`）。

本仓库由长夜维护增强版，并非原作者官方仓库。原有设置键与数据格式保留兼容，部分界面和图片目录仍使用“柏宝绘”名称。

## 安装

在酒馆打开 **扩展 → 安装扩展**，粘贴下面的地址：

```text
https://github.com/yjlovegy/changye-image
```

安装完成后刷新酒馆页面。进入插件的“工作流”，在“配置”中填写自己的 ComfyUI 地址并测试连接，再导入工作流、选择模型；仓库不附带模型、API 密钥或个人工作流。

**已有柏宝绘或 ZIP 修改版：请先停用旧版，再启用本版。不要同时启用两份，否则会重复处理消息和图片。** 建议先导出/备份旧版设置；本版沿用原设置命名空间，不主动清空配置。

## 更新

在酒馆扩展管理中点击本扩展的更新按钮，或在插件设置页检查更新，更新后刷新网页。版本检查与更新来源均指向本仓库。

通过 ZIP 复制的旧目录没有 Git 安装记录，需要先通过上面的链接安装本版，才能使用酒馆的 Git 更新功能。更新仓库代码不会替你下载新的模型，也不会自动更换你的工作流。

## 主要功能

- 根据剧情生成配图提示词，也可选中文字后手动配图。
- 人物固定外貌管理，支持脸型、五官和角色特征；画面动作、神态与位置关系随剧情描述。
- ComfyUI 工作流的模型、文本编码器、编码器类型、VAE、采样器、调度器、步数、CFG 和降噪参数可在插件中设置。
- 每个工作流独立保存固定正面前缀/后缀与固定负面提示词。
- LoRA 标签编辑与同步、共享 LoRA 收藏及备注。
- 自定义宽高、共享常用尺寸库、每个工作流独立默认尺寸；单张图片可选尺寸并重新生成。
- 工作流 JSON 上传、下载、粘贴及保存。
- 按修改意见调用副 API 生成新的正负提示词，确认后重新生图。
- 图库管理；可选 Windows 文件夹助手。

复杂姿态及人物一致性仍受底模、工作流、参考图和提示词影响，功能不保证每次出图完全一致。

## V1.2

- 在“工作流 → 模型与采样 → 提示词模式”选择 **Anima** 或 **Krea2**，点击“保存模型与采样设置”。每个工作流分别保存；旧工作流默认沿用 Anima 模式。
- Anima 继续使用核心标签与完整英文描述；Krea2 使用连贯英文描述，自动配图、选中文字配图和 AI 修改提示词均按所选模式生成。此选项只控制提示词，不替你更换模型或编码器类型。
- “设置 → 自定义提示词”分别提供 Anima、Krea2 的规范与输出前检查。原有 ComfyUI 自定义内容保留在 Anima 下，Krea2 可单独编辑，检查过程不作为最终输出。
- 新生成的图片记录提示词模式，编辑旧图时沿用其模式；Krea2 同一文本输入中同时存在 `%prompt%` 和 `%nl%` 时只填入一次描述。
- 导航“渠道”改名“工作流”，保留 ComfyUI URL 与测试连接；移除 NAI 切换及 NAI 自定义提示词界面。旧设置数据保留兼容，工作流、固定提示词、收藏和图片不清空。
- 主界面和设置标题统一为“长夜的绘图器”。

## v1.1.2

- 当前工作流下方新增小幅示例图，支持上传、点击放大、下载原图和移除，切换工作流时分别显示。
- 支持 PNG、JPG、WebP（单张不超过 20 MB），上传后自动保存，无需单独安装服务器助手。
- 图片保存在酒馆当前用户的 `data/<用户>/user/images/长夜的绘图器_工作流示例/`（自定义 dataRoot 时以实际数据目录为准）。插件升级不会覆盖这些图片；迁移电脑时需连同用户数据目录一起复制。
- 复制工作流时沿用示例图，各工作流可独立更换；只有没有其它工作流使用的旧图才会清理。示例图不参与生成请求，也不包含在工作流 JSON 下载中。

## v1.1.1

- 新增编码器类型设置；Krea2 工作流使用其要求的 `krea2` 编码器类型。
- 调度器与 CFG 对齐；完整保留已有模型和采样控件。
- 共享尺寸库与单图尺寸覆盖，旧横竖尺寸迁入收藏。
- 副 API 的“生成 tag 使用”调整为上下两行。
- GitHub 发行版更新地址改为本仓库。

## 可选：“打开文件夹”助手

普通酒馆扩展安装不会安装服务器插件。**仅需要在 Windows 本机点击图库后弹出资源管理器的用户才需要本节。**

1. 将本仓库 `server-plugins/baibai-image-folders` 文件夹复制到 `SillyTavern/plugins/baibai-image-folders`。
2. 在酒馆 `config.yaml` 中设置 `enableServerPlugins: true`。
3. 关闭原酒馆启动窗口，用原来的启动方式重新启动。
4. 使用 `http://127.0.0.1:8000` 或自己的本机酒馆端口访问。点击图库分组的“打开文件夹”；手动删减后重新读取图库。

远程访问不支持打开服务器桌面窗口。服务助手只接受当前酒馆用户已有的柏宝绘图片目录；不支持任意磁盘路径。已有文件夹修正版助手的用户无需重复安装。

## 开发

源代码在 `src`，酒馆直接加载已提交的 `dist` 文件，使用者不需要编译。

```sh
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm build
```

发布新版时同时提交 `package.json`、`manifest.json` 和重新构建的 `dist`。服务助手如有更新，需要手动同步并重启酒馆。

本次发行通过 TypeScript 检查、生产构建和自动测试；不包含个人聊天、角色卡、图库、接口凭据或个人工作流。模型的真实出图需在自己的环境中验证。

## 来源与署名

原作：[柏柏 / ST-BaiBai-Image](https://github.com/baibai-git/ST-BaiBai-Image)。增强版维护：[长夜 / yjlovegy](https://github.com/yjlovegy)。保留原作署名；本仓库不冒充原作者发行。

整理本版时，上游目录未附明确的 LICENSE 文件，因此这里未替上游添加 MIT 等授权声明。第三方依赖按其各自许可证使用。
