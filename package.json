{
  "name": "changye-image",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-version": "node scripts/sync-version.mjs",
    "prebuild": "node scripts/sync-version.mjs",
    "build": "vite build",
    "prewatch": "node scripts/sync-version.mjs",
    "watch": "vite build --watch --mode development",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run src && node --test server-plugins/baibai-image-folders/index.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "^5.7.0",
    "vite": "^7.3.3",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.3"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "vue": "^3.5.35"
  },
  "bbiBuild": "github1"
}
